Three condense calls that answer with claims as long as the replies they were given: the
condensed total is still over `SPLIT_MIN_CHARS`, so the comparison call is never made and the
turn degrades naming the largest block (`condense_ineffective`). There is deliberately no
fourth file: a comparison call here would be a bug.
