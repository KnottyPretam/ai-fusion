Refactor's four calls — the map call, then one per label — followed by the comparison Analyze
makes when it consumes the refactored artifact (the fifth file is analyst_split's Extraction
verbatim). Tests that only run Refactor use the first four.
