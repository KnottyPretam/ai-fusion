The analyst's first call produces some text and THEN a transport error; its second (the
correction attempt, non-web transport) errors with no text at all. Pins that a partial is
recorded exactly ONCE per attempt (Refactor resets its recorder between attempts) and that the
correction never echoes the partial as an assistant turn.
