Two condense passes. Three 9,000-character replies split into two pieces each (six first-pass
calls) whose claims still total ~24,600 characters, over CONDENSED_MAX_CHARS, so a SECOND pass
runs over the condensed blocks (six more calls, ~7,200 total) before the comparison (the
thirteenth file, analyst_split's Extraction verbatim). Sizes are what ChatGPT measured at on
2026-09-22: one pass reduces a reply by about thirty percent, not the seventy the old check assumed.
