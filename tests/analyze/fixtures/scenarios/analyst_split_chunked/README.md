One reply over `CONDENSE_CHUNK_CHARS` is condensed in THREE pieces, then the other two labels in
one each (five condense calls), then the comparison (the sixth file, analyst_split's Extraction
verbatim). Proves a chunked label reaches the comparison as ONE block of concatenated claims.
